import React from 'react';

const PrivateRoute = () => {
    return (
        <div>
            <h4>directed to to private route then redirecting to soughted page</h4>
        </div>
    );
};

export default PrivateRoute;